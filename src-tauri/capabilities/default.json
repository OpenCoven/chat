{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the OpenCoven Chat main window.",
  "windows": ["main"],
  "permissions": ["allow-app-identity"]
}
