{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the OpenCoven Chat main window: the Coven CLI runtime commands the chat UI invokes, and nothing else. The Cave adapter and identity commands stay registered for their native and conformance coverage but are not reachable from the webview.",
  "windows": ["main"],
  "permissions": [
    "allow-coven-runtime-status",
    "allow-coven-runtime-familiars",
    "allow-coven-runtime-sessions",
    "allow-coven-runtime-chat-lifecycle",
    "allow-coven-runtime-read",
    "allow-coven-runtime-send",
    "allow-coven-runtime-cancel"
  ]
}
